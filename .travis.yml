language: java
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/lib


install:
  - wget https://drive.google.com/file/d/${CPLEX_SHARE_FILE}
  - mv ${CPLEX_SHARE_FILE} $HOME/lib/cplex.jar
  - export CPLEX_JAR=$HOME/lib/cplex.jar

script: echo "not testing"

deploy:
    provider: script
    script:
        - build/deploy.sh $TRAVIS_BRANCH
    on:
        all_branches: true
        condition: $TRAVIS_BRANCH =~ ^dev|master$
